[package]
name = "talk-bytecode"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
talk-mir = { path = "../talk-mir" }
talk-vm = { path = "../talk-vm" }
rustc-hash = "2.1.1"
profiling = { version = "1.0.18", default-features = false }
