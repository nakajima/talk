package talkex:host;

world host {
   record highlight-token {
    kind:  string,
    start: u32,
    end:   u32,
  }

  export highlight: func(code: string) -> list<highlight-token>;
  export ir: func(code: string) -> string;
  export run: func(code: string);
  export ping: func();
}
