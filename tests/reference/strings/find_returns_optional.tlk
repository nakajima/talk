match "hello world".utf8().find_bytes("world") {
	.some(i) -> print_raw(i.show()),
	.none -> print_raw("none")
}
match "hello".utf8().find_bytes("zzz") {
	.some(i) -> print_raw(i.show()),
	.none -> print_raw("none")
}
