match "hello world".find(needle: "world") {
	.some(i) -> print_raw(i.show()),
	.none -> print_raw("none")
}
match "hello".find(needle: "zzz") {
	.some(i) -> print_raw(i.show()),
	.none -> print_raw("none")
}
