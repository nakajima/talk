func take(name: String) -> Int {
	name.byte_count
}
func f(s: String, flag: Bool) -> Int {
	let n = 0
	if flag {
		n = take(name: s)
	}
	n
}
f(s: "hello" + " world", flag: true) + f(s: "bye" + " now", flag: false)
