func observe(s: &String) -> Int {
	s.byte_count
}
func mutate(s: &mut String) -> Int {
	s.byte_count
}
func ok() -> Int {
	let s = "hello" + " world"
	let i = 0
	loop i < 2 {
		let r: &mut String = s
		let n = observe(r)
		let m = mutate(s)
		i = i + 1
	}
	0
}
