struct Node 'heap {
	let value: Int
	let next: Node?
}
func check() -> Int {
	let a = Node(value: 1, next: Optional.none)
	let b = Node(value: 2, next: Optional.none)
	a.next = Optional.some(b)
	0
}
check()
