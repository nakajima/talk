use dict::{ Dict }

func check() -> Int {
	let d = Dict<String>()
	d.insert(key: "greet", value: "hello" + " world")
	let got: Optional<&String> = d.get(key: "greet")
	match got {
		.some(s) -> s.byte_count,
		.none -> 0 - 1
	}
}
check()
