func choose(a: &String, b: &String) -> Substring {
	a.utf8().slice(start: 0, byte_count: 1)
}
func bad() -> Int {
	let a = "hello" + " world"
	let b = "bye" + " now"
	let sub = choose(a: a, b: b)
	sub.byte_count
}
