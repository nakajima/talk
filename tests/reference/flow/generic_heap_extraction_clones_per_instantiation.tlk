struct Holder<Value> 'heap {
	let value: Value
}
func extract_clone<Value: Clone>(h: Holder<Value>) -> Value {
	h.value
}
func extract_copy<Value: Copy>(h: Holder<Value>) -> Value {
	h.value
}
func check() -> Int {
	let h = Holder(value: "hello" + " world")
	let s = extract_clone(h: h)
	let i = Holder(value: 41)
	let n = extract_copy(h: i)
	s.byte_count + n
}
check()
