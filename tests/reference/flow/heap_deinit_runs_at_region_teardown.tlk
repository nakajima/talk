let counter = 0
struct Guard 'heap {
	let id: Int
}
extend Guard: Deinit {
	consuming func deinit() -> Void {
		counter = counter + 1
		()
	}
}
func scope() -> Int {
	let g = Guard(id: 1)
	let h = Guard(id: 2)
	0
}
let n = scope()
counter
