struct Box {
	let value: String
	mut func touch() -> Int {
		self.value.byte_count
	}
}
func bad() -> Int {
	let box = Box(value: "hello" + " world")
	let sub = box.value.utf8().slice(0, 1)
	let n = box.touch()
	sub.byte_count + n
}
