struct Box {
	let value: String
	mut func touch() -> Int {
		self.value.byte_count
	}
}
func ok() -> Int {
	let box = Box(value: "hello" + " world")
	let sub = box.value.utf8().slice(start: 0, byte_count: 1)
	let n = sub.byte_count
	box.touch() + n
}
