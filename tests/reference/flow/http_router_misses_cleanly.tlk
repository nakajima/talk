func check() -> Int {
	let server = HTTP.Server()
	server.get(path: "/", handler: func() -> String { "home" })
	let wire = server.handle(raw_request: "GET /nope HTTP/1.1")
	match wire.find(needle: "404") {
		.some(i) -> i,
		.none -> 0 - 1
	}
}
check()
