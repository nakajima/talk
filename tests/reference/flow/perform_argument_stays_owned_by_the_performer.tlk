effect 'log(msg) -> Int
@handle 'log { msg in
	'continue msg.byte_count
}
func f(flag: Bool) 'log -> Int {
	let s = "hello" + " world"
	let n = 0
	if flag {
		n = 'log(msg: s)
	}
	n
}
f(flag: true) + f(flag: false)
