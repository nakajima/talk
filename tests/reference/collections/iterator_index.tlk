let xs = [10, 20, 30]
match xs.iter().index(needle: 20) {
	.some(i) -> print(i),
	.none -> print("not found")
}
match xs.iter().index(needle: 99) {
	.some(i) -> print(i),
	.none -> print("not found")
}
