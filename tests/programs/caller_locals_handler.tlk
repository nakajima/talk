effect 'fail(message: String) -> Never

func risky(n: Int) {
	if n > 2 {
		'fail(message: "too big")
	}
	print("ok")
}

func attempt(n: Int) {
	let prefix = "caught: "
	#handle 'fail { message in print(prefix + message) }
	risky(n: n)
	print("after risky")
}

attempt(n: 1)
attempt(n: 5)
