effect 'ask<T>(value: T) -> T

func probe<T>(consume x: T) -> T {
	'ask(value: x)
}

func run() -> Int {
	@handle 'ask { v in 'continue v }
	let n = probe(x: 21)
	let b = probe(x: true)
	let direct = 'ask(value: n + n)
	if b {
		direct
	} else {
		0
	}
}

print(run())
