[package]
name = "talk-static"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
talk-runtime = { path = "../talk-runtime" }

[lints.clippy]
approx_constant = "allow"
