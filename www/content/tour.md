# talktalk: a tour

## TYPES

### PRIMITIVES

#### `Int`

signed usize. everyone's fav. it can count stuff. it can be negative. if you haven't used it you're missing out.

#### `Byte` 

a lil Int.

#### `Float`

i have not spent much time with this one. needs more investigation. i think it has something to do with fractions.

#### `Bool`

do u like bools? circle one: `true`/`false`

#### `()` (aka `Void`)

an absolute unit type.

#### `Never`

for when you can't even.

### USER DEFINED (that's you!)

#### struct

#### enum


```talktalk
let foo = 123
```
