A 64-bit IEEE 754 floating-point value.

Arithmetic follows IEEE 754 behavior, including infinities, signed zero, and NaN. Consequently, equality is not reflexive for NaN and ordering is partial rather than a total ordering over every bit pattern.
