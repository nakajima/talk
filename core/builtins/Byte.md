An unsigned 8-bit integer used for raw bytes and UTF-8 storage.

`Byte` is distinct from `Int`; conversions are explicit. Arithmetic and bitwise behavior comes from its Core extensions, and values always remain in the range 0 through 255.
