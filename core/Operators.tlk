protocol Adds<Operand, Returns> {
  func adds(op: Operand) -> Returns
}

extend Int: Adds<Int, Int> {
	func adds(op: Int) -> Int {
		self + op	
	}
}

extend Float: Adds<Float, Float> {
  func adds(op: Float) -> Float {
    self + op
  }
}