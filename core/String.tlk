struct String {
	let base: RawPtr
	let length: Int
	let capacity: Int
}

extend String: Add {
	func add(rhs: String) -> String { self }
}

extend String: Equatable {
	func equals(rhs: String) -> Bool { false }
}
