func _alloc<Element>(count: Int) -> RawPtr {
	@_ir(count) { %? = alloc Element $0 }
}

func _load<Element>(ptr: RawPtr) -> Element {
	@_ir(ptr) { %? = load Element $0 }
}

func _copy(from: RawPtr, to: RawPtr, length: Int) -> Void {
	@_ir(from, to, length) { copy Byte $0 $1 $2 }
}

func _ptr_add(ptr: RawPtr, offset: Int) -> RawPtr {
	@_ir(ptr, offset) { %? = add RawPtr $0 $1 }
}
