struct Array<Element> {
	let count: Int
	let capacity: Int
	let storage: RawPtr

	init(capacity: Int) {
		self.capacity = capacity
		self.count = 0
		self.storage = @_ir { %? = alloc Element %0 }
	}
}
